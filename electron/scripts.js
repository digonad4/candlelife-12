
// This file contains commands that will be added to package.json scripts
// through the Lovable UI since we can't modify package.json directly

// These are the scripts that would be added to package.json:
/*
  "dev:electron": "concurrently \"cross-env ELECTRON=true vite\" \"wait-on http://localhost:8080 && electron electron/main.js\"",
  "build:electron": "cross-env ELECTRON=true vite build && electron-builder -c electron-builder.json",
  "preview:electron": "electron electron/main.js"
*/

// This is just a placeholder file for documentation since we can't modify package.json directly
